# =================
# Plugins
# =================
# You can import plugins using the use directive.
# To install a plugin, use the command: "devalang addon install devaloop.acid"
#
# Syntax:
# `use <author>.<plugin> as <alias>`

use devaloop.acid as acidPlugin

# Now you can use the plugin's features via the alias "acidPlugin"
const acidPluginSynth = synth acidPlugin.synth

# Play a note using the plugin synth
acidPluginSynth -> note(C4)
                -> velocity(100)
