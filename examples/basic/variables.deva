# Variable declarations and types
bpm 120

# Number variables
let count = 4
let tempo_value = 140
let volume = 0.8

# String variables
let track_name = "Main Theme"
let instrument = "synth"

# Boolean variables
let is_playing = true
let mute = false

# Array variables
let notes = [C4, E4, G4]
let durations = [250, 500, 750]

# Map variables (synth definitions)
let my_synth = synth sine {
    attack: 0.01,
    decay: 0.1,
    sustain: 0.7,
    release: 0.2
}

print "Variables defined"
